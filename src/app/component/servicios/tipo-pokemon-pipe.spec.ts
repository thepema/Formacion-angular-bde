import { TipoPokemonPipe } from './tipo-pokemon-pipe';

describe('TipoPokemonPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoPokemonPipe();
    expect(pipe).toBeTruthy();
  });
});
